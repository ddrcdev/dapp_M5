El archivo oracleWorker contiene lo necesario para implementar el contrato Oracle.sol en la capa de cliente. 
La llamada a la función Oracleworker puede realizarse en el mismo useEffect implementado.

En la carpeta Traditional_query se encuentran dos archivos correspondiente a la implementación tradicional de API mediante .js y python.
